import QtQuick

Item {
    id: root

    width:parent.width
    height: 250

    // 标题
    Rectangle {
        id:titleRec

        Text {
            id: titleText
            text: qsTr("动作时间轴")
        }
    }
    // 按钮
    // 时间尺
    // 时间轴
}
